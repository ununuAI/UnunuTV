task_id: 20260718-original-ui-parity
goal: Restore the original ununu-web UnuTV interaction model and guarantee that both new projects and migrated projects use the same formal application/API/SQLite persistence paths.
done_when: Original node cards, anchored Prompt controls, free-flow edges, viewport zoom, Script workspace, Director workspace, panorama, assets, workflow, and timeline behave like the legacy reference; every durable Web action delegates to an API/core use-case; a fresh project survives runtime close/reopen with project, canvas, typed node state, edges, groups, media, assets, workflow, Director versions, panorama, reviews, runs, and timeline intact; migration calls those same use-cases; same-state browser design QA has no P0/P1/P2 mismatch.
scope: packages/contracts, packages/core, packages/local-runtime, apps/api, apps/web, migration scripts, tests, and parity evidence; video-production surfaces only; no login, cloud project storage, generic web/PPT/design features, paid provider execution, destructive removal of legacy data, or compatibility UI presented as completed domain migration.
verify: npm run verify; fresh-project close/reopen integration gate; migration idempotency and semantic-count audit; browser interaction probes for zoom/edges/Prompt/Script/Director; same-viewport combined-image comparison against the five owner screenshots and original source captures; design-qa.md final result passed.
status: repair
evidence: Owner acceptance found blocking mismatches: migrated legacy data was stored mainly as compatibility node payloads; Director and Script surfaces were replaced by simplified custom placeholders; selected-node Prompt controls did not match the original; smoothstep edges created an orthogonal wire grid instead of the original free-flow curves; the displayed zoom percentage was not proven to track the real ReactFlow viewport. The previous accepted result remains withdrawn. Repaired now: the exact legacy Director console and ScriptV2 production workspace are mounted against formal API data; PromptCard, reference controls, model selectors and paid confirmation are restored; ReactFlow zoom and percentage are linked; free bezier edges are restored; legacy storyboard/shot group nodes render as the original transparent group frames instead of fake cards; the exact project/global subject-resource panel reads real SQLite assets; right-click asset promotion opens the original create/add-version dialog with explicit role and scope. Repair migration formally stored 275 Prompt documents, 40 Script rows, 3 short-drama production documents and Director stages in version tables, removed duplicated domain payloads, and reruns with zero changes. The canvas menus, home panels, asset use-cases and asset SQLite adapter were split into atomic modules without changing behavior. The collapsed script/director preview now rejects full Prompt bodies and clips defensively, fixing the owner-reported screenplay overflow; migrated text artifacts fall back to their legacy Prompt body so L01/L02/L04 no longer appear empty. `npm run verify:arch`, `npm test` (9/9), and `npm run build` now pass. Browser probes confirm the project route, real project/global asset counts, global audio asset, complete legacy add-node menu, restored 1,220-character L01 body, concise L03 preview, and no fresh warning/error.
next: Finish same-state comparison for remaining canvas chrome and project-home interactions and run the final browser evidence set. The isolated fresh-project close/reopen gate already covers typed nodes, Prompt versions, Script rows, edges, groups, media, assets, workflow, Director versions, panorama, reviews, runs and timeline; final parity remains unaccepted until the remaining visible chrome comparison is signed off.
