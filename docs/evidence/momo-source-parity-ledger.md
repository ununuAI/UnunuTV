# Momo source parity ledger

This ledger is the required source of truth for recreating Momo interaction behavior in UnunuTV. A screenshot may clarify appearance, but it is not sufficient implementation evidence. Every parity entry must cite local bundled source or an extracted binary module and must label deliberate Ununu additions.

## Evidence roots

- Analysis: `/Users/zhangxiaohao/Ununu/电影工业控制/video-analysis/momo-source-2026-07-20/momo-source-interaction-and-agent-analysis-v0.1.md`
- Formatted flow bundle: `/Users/zhangxiaohao/Ununu/电影工业控制/video-analysis/momo-source-2026-07-20/extracted/momo-flow-index-DZBEYAr3.js`
- Workflow and external-source audit: `docs/evidence/momo-and-external-cinematic-orchestration-audit-20260721.md`
- Installed application: `/Applications/momo.app`

## Verified parity entries

| Surface | Momo source evidence | Verified behavior | Ununu implementation decision | Verification |
| --- | --- | --- | --- | --- |
| Left capsule toolbar | Flow bundle `SideToolbar` (`rNI`) around lines 152400-153125 | A persistent left capsule owns add, asset library, workflow library, history, canvas tools and home. Only one toolbar popover is open at a time; add emits a typed node request and the other entries open compact side popovers. | Reproduce the persistent capsule and mutually exclusive compact panels. Keep Ununu project/film resources inside those panels; do not replace them with full-screen workspaces. | Browser DOM exposes `画布主工具栏`; add/library/workflow/history/navigation/home interaction probes. |
| Bottom canvas controls | Flow bundle `CustomControls` (`QJI`) around lines 153953-154330 | A bottom-left translucent pill toggles edges and minimap, fits the viewport, binds the live VueFlow zoom to a 0.1-2 slider, and opens timeline/player/shortcut-assets panels. | Reproduce the same control grouping against the real ReactFlow viewport. Ununu keeps the wider supported zoom range but the slider and displayed percentage always reflect the live viewport. | Browser DOM exposes `画布显示与工作区控制`; viewport/slider/fit/panel state tests. |
| Canvas zoom and information density | `CustomControls` watches `viewport.zoom` in both directions around lines 153979-154014; Momo nodes remain native canvas objects while the viewport scales. | Zooming never leaves the canvas or swaps to a separate page. | Preserve native canvas scaling and add the explicit Ununu `overview/summary/detail` projection so industrial nodes remain legible at large project scale. This density projection is labelled an Ununu industrial enhancement. | `canvas-node-view-policy.test.mjs` and fixed-viewport zoom captures. |
| Canvas-inline expansion | Flow layout around lines 209390-209669 keeps nodes, player and timeline in one canvas workspace; large asset/storyboard surfaces remain canvas content rather than a replacement full-screen route. | Work continues in the same spatial canvas; only the player may Teleport above it. | Script, director, storyboard, shot, generation-unit and QA nodes expand in place, remember their compact size and refocus through ReactFlow. `影视总控` is deliberately project-level and opens from the left capsule as a canvas work window; it is no longer a canvas card. This is an explicit Ununu industrial enhancement of the Momo canvas rule. | Canvas-view transition tests, `canvas-entry-policy.test.mjs` and fixed-viewport work-window probes. |
| Node registry | Flow bundle around the node registry near line 27770 | Text, Image, Video, Audio, Grid, Asset, ImageEdit, Compare, World and Director nodes are registered as distinct types. | Reproduce the registry and add Script/Story, Storyboard, Shot/Generation Unit and Review/QA as explicitly labelled Ununu canvas additions. The cinematic contract remains in the internal presentation registry for durable compatibility, but its project controller cannot be added to or rendered on the canvas. | `canvas-entry-policy.test.mjs`, node-menu behavior tests and fixed-viewport capture. |
| Agent control plane and deterministic execution plane | Flow Agent tools around lines 17249-17268; per-session Agent state around 17270-17480; group DAG executor around 19344-19515; persisted `isFullAuto` and `FlowAgentInput` around 32261-32440 | `chief/planner/prompter/asset_designer` decide stages and call bounded tools, while a separate edge-derived DAG executes ready Text/Image/Asset/Video/World nodes, supports parallel waves, failure propagation and AbortController cancellation. Full-auto is a persisted session mode over the same tools, not a separate opaque pipeline. | Preserve this two-plane separation without copying the AI chat. Ununu's 13-stage automation DAG is the observable execution plane; professional contributions and Core use cases are the decision/artifact boundary. Agents cannot write persistence, call Providers or spend budget outside the same Core/API/CLI use cases. | `automation-task-dag.test.mjs`, `automation-executor.test.mjs`, `automation-recovery.test.mjs`, Core boundary checks; detailed source map in `momo-and-external-cinematic-orchestration-audit-20260721.md`. |
| Storyboard image/video orchestration | Image workflow around lines 30680-31035; video workflow around 31100-31530 | Both workflows skip existing results, surface per-card state, cancel, retry and support fully-linear, scene-concurrent/scene-linear and all-concurrent strategies. They confirm the first image/video globally or per scene before continuing; image cards retry five times, video cards three times. | Keep the three scheduling modes and visible activities, but place paid dispatch behind Ununu owner approval, idempotency and budget gates. First/scene-first approval is a production checkpoint, not proof of spatial, identity or action continuity; those remain explicit Ununu QA gates. | Storyboard batch/retry/cancel tests, automation activity projection and the 20260721 orchestration source audit. |
| Project-level cinematic controller entry | No direct Momo counterpart; this is an explicit Ununu film-industrial addition constrained by Momo's persistent left-capsule/canvas-workspace rule. | One project controller entry should not compete with a duplicate draggable card. Large project work windows should open from a consistent canvas anchor and stay below the detached player. | The left capsule is the only visible `影视总控` entry. Legacy cinematic node records remain a hidden durable binding so productions/contracts are not lost, while presentation and add policies exclude the card. `影视总控` and `全自动生产流` share one versioned default frame (origin, width and height); at 1536×900 it is 1180×800. Full-auto stays at z-index 104 and the player at 110. | `canvas-entry-policy.test.mjs`, `cinematic-control-window-policy.test.mjs`, `automation-flow-window-policy.test.mjs`; full 219-test/architecture/build gate. |
| Accepted-video retention across production-plan replay | No direct Momo source counterpart; this is a live Ununu film-industrial recovery addition. | Replanning may supersede or quarantine a failed candidate, but it must not make an already usable canvas video disappear before a replacement exists. | The hybrid plan only clears `currentMediaId` when it belongs to rejected/quarantined media. P01A is bound to accepted render `media-20de8005-c758-477a-b478-7efeb7a5bafa`; Browser confirms the visible video src after reload. | Live project Browser replay; P01A media/evaluation lineage; full 228-test/architecture/build gate before the later P01B compatibility patch. |
| Frame-input versus ordinary-reference capability gate | No direct Momo source counterpart; explicit Ununu Provider-safety addition based on a real Ark rejection. | Seedance frame mode and ordinary multi-reference mode are separate request shapes. A known-invalid combination must be stopped before paid submission. Planning/Director evidence may remain auditable without being sent as an ordinary Provider image. | Registry version `2026-07-21` marks Ark Seedance as forbidding ordinary references with either first-frame or first-last-frame input; Core preflight and Ark adapter both enforce it. Compilation now filters ordinary Director/storyboard/authority bindings out of the Provider list when frame input is active while retaining Director references in `sourceVersions`. P01B is back on legal `first_frame` with the sole accepted P01A tail image; preflight is `ready:true`. | `cinematic-compilation-context.test.mjs`, `video-model-capability-policy.test.mjs`, `providers.test.mjs`; focused 26/26 and full 240/240/architecture/build gate; no new Provider submission. |
| Authoritative accepted-tail handoff | No direct Momo source counterpart; explicit Ununu film-continuity addition informed by LocalMiniDrama/Jellyfish/Toonflow/ViMax/deep-printfilm local source. | A continuous segment must begin from the actual accepted prior media state, not a storyboard approximation or a Prompt sentence. The handoff must preserve subject position/state, spatial axis and screen direction. | `ReferenceBinding` carries source evaluation/media/checksum/extracted-second and handoff verification. Core derives `authoritativeTailHandoff` only from the matching ACCEPT evaluation and exact media checksum. `PREVIOUS_ACCEPTED_TAIL` also requires the GenerationUnit first-frame ID to match. P01B uses the 3.9-second P01A tail; its earlier candidate is REJECT for both entrance-to-mid-hall teleport and three-to-two talisman collapse. | `cinematic-compilation-context.test.mjs`, `cinematic-prompt-policy.test.mjs`, `media-preparation.test.mjs`; live compilation `prompt-compilation-172a9805-a22b-4fa9-a123-dffe4189f121`, evaluation `cinematic-evaluation-bloodmoon-p01b-spatial-handoff-reject-v2`. |
| Provider inputs versus workflow connections | Momo rich slots distinguish atomic Provider references from graph structure; Ununu additionally has formal Director/authority lineage that may remain connected without being sent to a model. | A canvas edge is auditable workflow context, not automatically a Provider image in text/frame modes. Visible role labels and the request must describe the same media. | Web and `generationRunPayload` now share a mode-aware reference policy. P01B first-frame mode shows the Core-locked P01A tail as `首帧/核`; six ordinary authority/Director connections remain visible as `未使用` and never enter the Provider payload. Image-reference mode still includes selected connected/explicit images. A rejected playable candidate also carries an explicit warm-red rejection state instead of appearing accepted. | `node-provider-reference-policy.test.mjs`, `generation-run-payload.test.mjs`, `media-candidate-policy.test.mjs`; in-app Browser fixed-state replay; full 240/240/architecture/build gate. |
| World node | Flow bundle `WorldNode` (`JSI`) around lines 141838-142590; size policy around 27304-27305 | Default node is 333×250. It resolves the current world cover, up to four distinct history worlds, quality selection, open/focus/export actions and an executable World Prompt. Collapsed history uses rotated/scaled cards; expanded history uses exact offsets `(516,0)`, `(0,-266)`, `(516,-266)` with later entries falling back to the first slot. Preview size follows the current cover aspect instead of being forced to 100%. | Reproduce the native surface, exact sizes, quality/open/export controls, durable media lineage and history gestures. Because no verified World Provider adapter exists, the generation Prompt is hidden rather than exposed as a dead generic composer. Imported SPZ remains a real versioned World authority, and a bound World without a cover reports that it is ready for Director connection instead of claiming it is empty. | `native-media-node-policy.test.mjs`, `prompt-output-mode-policy.test.mjs`, `canvas-node-selection-policy.test.mjs`, final 219-test/architecture/build gate; 1440×900 Browser/computed-style verification of 333×250 and all three offsets. Visual comparison: `docs/evidence/screenshots/20260720-world-reference-comparison.png`. Real Provider execution remains blocked by the named capability absence. |
| 3D Director and Gaussian World binding | Flow bundle Spark integration around lines 122554-124713; Director pose/camera/splat lifecycle around 130204-131076; Director enter/exit surfaces around 138261-138966 | Director owns a genuine 3D viewport, camera state and splat lifecycle; the World and Director remain distinct canvas node types. | Preserve the native distinction and add explicit Ununu industrial authority: a versioned World asset/media/checksum and transform bind through `set_environment`; users and Agents share revisioned/idempotent Director Stage commands; blocking, routes and cameras are durable; capture binds the exact stage revision before it becomes a shot/storyboard control plate. Spark automatic accumulator rebuild is disabled and the WebGL runtime is isolated from unstable React callback identities so static splats do not flicker. | `director-stage-command-policy.test.mjs`, `director-stage-command-persistence.test.mjs`, `director-cinematic-binding.test.mjs`, `native-media-node-policy.test.mjs`; Browser captures `05-world-gaussian-director.png`, `06-agent-blocking-in-gaussian-world.png`. Final 1440×900 replay produced identical SHA-256 hashes across three full-viewport frames spaced 1.15s apart; comparison: `docs/evidence/screenshots/20260720-director-reference-comparison.png`. |
| Director capture node placement | Momo keeps generated media as independent canvas nodes; no source evidence says repeated exports should share one coordinate. This is an explicit Ununu industrial layout addition, not screenshot-inferred Momo behavior. | Re-exporting one camera updates its existing artifact; exporting a new camera produces an independently operable image node that does not cover prior nodes. | Existing camera/variant identity is matched first and preserves its current position. New captures use camera order, a stable three-column grid and collision search. Occupancy uses a 559×720 footprint so the selected-only Prompt does not overlap the following row. | `director-export-placement-policy.test.mjs`; real `project-696…` S01–S14 nodes verified at distinct 3×N coordinates through CLI and refreshed in the in-app Browser. |
| Audio node | Flow bundle `AudioNode` (`TSI`) around lines 142610-142805; default size at 27217-27224 and 27304 | Default node is 444×250. A real audio URL exposes a 60px waveform, duration/time display, play/pause/finish state, upload/open actions, and drag-start/drag-end integration with the bottom timeline. Empty audio shows a dedicated empty state. | Reproduce the exact surface, real media, 96-bar local waveform, duration/playback and selected toolbar. The waveform hook owns one `AudioContext` lifecycle and never double-closes it. Timeline dragging serializes exact node/media/duration identity and the drop calls the durable Timeline core use case with 100ms snapping; no clip is invented only in React. | `native-media-node-policy.test.mjs`, restart persistence test, `timeline-drag-policy.test.mjs`; 1440×900 Browser computed 444×250, two-second duration, 96 bars and an enabled `draggable=true` timeline affordance. The same media was persisted on A1, survived service restart and completed local media preparation. |
| Compare node | Flow bundle `CompareNode` (`EkI`) around lines 142845-143520; defaults are also registered around lines 27290-27299 | Initial durable state is `sliderPosition: 50`, `swapLayer: false`, `splitDirection: vertical`. The first two incoming Image/ImageEdit/Video/Asset media form the pair; zero/one input shows a whole-node empty state. Sole selection exposes swap, split direction, reset and—when video is present—synchronized play/pause, mute and timeline seeking. The divider drags on the corresponding axis and clamps to 0-100; deselection or viewport release pauses video. | Reproduce the pair resolution, empty states, selected toolbar, labels, divider, synchronized playback/mute/seek and durable controls. Full-auto read-only keeps playback/seek/mute observable but disables payload mutations. Ununu deliberately retains the typed left/right connection contract requested for all production nodes, rather than copying Momo's disabled Compare output handle. | `compare-node-policy.test.mjs` and `compare-node-persistence.test.mjs` cover empty/single/pair resolution, axis/clamp/swap/reset, synchronized media controls, read-only policy and two real media across restart. Source structure and the common selected-node/toolbar visual shell are frozen in this ledger; no screenshot-only behavior is inferred. |
| Grid node | Flow bundle `GridNode` (`opI`) around lines 146371-146820; layout/aspect registries around lines 18115-18205; default data around lines 27229-27245 | Initial state is a 2×2 grid, empty cells and 1:1 aspect. Source-confirmed layouts are 2×2, 2×3, 3×3, 3×4 and 4×4; ratios are 1:1, 16:9, 9:16, 4:3 and 3:4. Every cell owns an invisible `cell-N` target while a connection is active. Sole selection exposes edit, layout, ratio, compose and clear. Connecting an image fills the addressed cell; layout changes preserve in-range cells; compose creates a new image node; empty compose warns and does nothing. | Reproduce the per-cell semantic targets as durable `grid-cell:N` edge roles, the selected toolbar, Momo's 250px aspect-derived sizing, clear and local composition. Ununu's explicit enhancement is deterministic local FFmpeg composition and restart-stable lineage (`sourceGridNodeId`, source media IDs, generated media and `generated` edge), with no paid Provider call. | `grid-node.test.mjs` proves the exact source registries, target roles, sizing, empty behavior, latest-per-cell resolution, real FFmpeg composition and two full runtime restarts. The common toolbar/connection shell has fixed-viewport evidence; no unsourced Grid behavior remains. |
| Rich Prompt tokens | Flow bundle `referenceSlot` handling around lines 18447-18604 | Prompt content is structured ProseMirror JSON with atomic reference slots, inline icon/thumbnail/title and media-type color. | Add `PromptDocumentV1`; bind tokens to stable Ununu asset/version/media lineage. Both legacy `（参考图N）` and the Momo-aligned `参考图N「权威素材名」 = 本帧语义别名。` compile into atomic tokens; the visible title stays rich while the short alias remains ordinary editable text. Display names are never Provider identity. | `prompt-document-v1.test.mjs`, Provider request fixtures and real project Browser replay: S01 shows four tokens in-node and the same four in the medium expanded Prompt window. |
| Asset Prompt output purpose | Momo Prompt surfaces vary their controls and task request by node/media type; the extracted node registry keeps Text, Image, Audio and Video as separate production purposes rather than one generic textarea. | The active generation purpose determines placeholder, model controls, reference affordances and submitted result type. | Explicit Ununu industrial enhancement: one asset authority may intentionally request text, image, audio or video output. The selected-only Prompt exposes an atomic output-purpose selector; changing purpose persists `outputMode`, swaps type-specific copy/model defaults and creates a typed downstream media node on submission. No mode is inferred from the asset title. | `prompt-output-mode-policy.test.mjs`, selected-asset Browser probe and image→audio→image state transition. |
| Asset authority operations | Momo asset/reference surfaces establish reusable media selection; project/season/episode/scene/shot authority is an explicit Ununu cinematic addition. | Reusable references remain distinct from display names and current thumbnails. | `AssetAuthority` now owns scoped search, exact get/save/update, story-risk routing, deterministic story-derived batch creation, atomic approval, version history/restore, usage/impact and compile behavior. Web/CLI/Agent paths call the same Core use cases; accepted authority lineage is never rewritten from React. | `asset-authority-operations.test.mjs`, cinematic contracts and API/CLI boundary tests cover derive/search/batch/approve/history/restore/impact/compile across restart. |
| Reference serialization | Flow bundle `referenceSlot` serialization and task request construction around the same Prompt modules | Display tokens serialize to stable internal references, then Provider payloads receive actual image/audio/video URL arrays. | Resolve `ReferenceToken` through `ReferenceBinding`; preserve `controls`, `doesNotControl`, accepted authority revision and final payload order. | Contract/compiler/adapter tests. |
| Single-keyframe still Prompt | Momo's rich Prompt and storyboard surfaces establish structured references; the extracted bundle does not establish Ununu's film-contract compiler. This is an explicit Ununu industrial addition. | A still used as a video keyframe represents one exact instant. It must not collapse a complete multi-beat action, edit timeline or internal execution records into one image. | `ununu.storyboard.keyframe.v1` emits short reference mappings, one frozen moment, space/subject/camera/performance/light/continuity lines and explicit prohibitions. It strips action chains, future beats, Director runtime snapshots and identifiers; more than five GPT Image 2 references blocks before payment. Director captures and derived anatomical close-ups receive distinct authority lineage so UI/provider deduplication cannot erase spatial or creature-rule evidence. | `cinematic-image-prompt-policy.test.mjs`, `storyboard-provider-dispatch.test.mjs`, `prompt-document-v1.test.mjs`；早期 221/221 验证了编译器。KF01 r3 `media-742ccf1a-52ca-4068-a06c-d5a39e86293b` 曾由早期 review 接受，但真实像素复核发现普通正脸/头肩转向，已被最新 `review-04db9509-5eee-465a-a18c-4ead0c808ed4` 覆盖为 REJECT；不得再作为当前成功证据。 |
| Cinematic screen-direction sign gate | No direct Momo source counterpart; this is an explicit Ununu film-industrial QA addition and is not inferred from screenshots. | A declared entrance/target/exit axis remains stable across keyframes and video. Subject motion cannot visually reverse merely because identity, props and set topology are otherwise correct. | Before accepting a keyframe, freeze the named screen zones and compare each active subject's hips/shoulders, leading foot, gaze, weapon tip, motion trail and VFX direction to the signed axis. Any retreat reading, mirrored entrance/exit, ambiguity or contradiction rejects the media and makes the GenerationUnit stale before another paid call. | Real project audit rejected KF02 v7 as `review-6ee92c6c-a3c2-4eb5-9b69-53a3e088c7fb`, quarantined its first paid video as `review-79b37780-4770-4133-8b10-fdd4c1227ef5`, removed the bad binding and observed GenerationUnit preflight `ready:false`; v8 visual replay pending. |
| Action-origin causality gate | No direct Momo source counterpart; this is an explicit Ununu film-industrial QA addition and is not inferred from screenshots. | Props, projectiles, contact effects and VFX must visibly belong to the named initiator instead of merely sharing the correct global direction. | Audit `initiator → hand/weapon/contact point → prop/projectile → visible trail/impact → target`. A near endpoint attached to the camera, frame edge, another actor or empty space rejects the frame and invalidates downstream GenerationUnit compilations. | Real project audit rejected KF02 v8 as `review-7b58e8a9-e2c0-4fad-9e3e-cd35c4c957fd`: its three talismans/trails started at the camera-side bottom edge instead of Baili's right-hand/foot-front fan. GenerationUnit returned to `ready:false`; v9 visual replay pending. |
| Irreversible identity-state continuity gate | No direct Momo source counterpart; this is an explicit Ununu film-industrial QA addition and is not inferred from screenshots. | Once a reveal, transformation, injury or costume/state change occurs, later frames retain that state unless a prior visible reversal causes another transition. | Preserve count, position, wardrobe and the view-dependent abnormal anatomy. A revealed corpse-puppet crowd cannot normalize into living human faces in the next action frame; for this production, front views remain blank and full wrinkled faces exist only on the rear skull. | Real project audit added a second rejection reason to KF02 v8 after ordinary living faces replaced the revealed corpse-puppet group. The ambiguous v9 batch `storyboard-batch-0aa68f69-47d9-491d-9f0c-42337d1989d1` was cancelled; v10 replay pending. |
| External generation-state reconciliation | Momo shows live per-node generation state; its local bundle does not define Ununu's separate CLI/API observer lifecycle. | A node shows processing only while a real local submission or persisted Provider run is active. A terminal run removes the overlay and reveals the resulting version without requiring a page reload. | The Web reconciles persisted runs every 2.5 seconds, preserves local pre-dispatch state, refreshes media on terminal transition and suppresses repeated error notices. Core treats a queued synchronous submission without an async task id as in-flight instead of failing it during observer polling. | `generation-run-recovery.test.mjs`, `local-runtime.test.mjs`; Browser replay observed KF01 `1 → 0`, KF02 active `0 → 1` and the failed v12 state returning to `0`/“待处理”; full 226/226 gates. |
| Long-running paid image recovery | Momo's bundle establishes visible generation state but does not define Ununu's separate paid Gateway timeout semantics. This is an explicit Ununu industrial safety addition. Legacy local evidence is `ununu-web/apps/api/src/adapter/ununu-image-provider.mjs` around lines 84-101: GPT Image 2 multi-reference edits may take several minutes, configurations below 300 seconds are rejected and the default is 1,800 seconds. | A long synchronous image edit remains active beyond 100 seconds. Transport loss, Gateway 5xx or a truncated success body cannot be treated as a known free failure because the Provider may already have accepted or completed the paid request. | `ununu-image-response-adapter.mjs` owns the 5-minute floor/30-minute default and complete response parsing. Every request carries a deterministic `x-request-id`; network loss, timeout, 5xx and malformed 2xx become `paid_submission_outcome_unknown`, preserve the reserved budget and trace details, block automatic retry and require explicit reconciliation. A batch cancelled during an in-flight Provider call quarantines a late artifact instead of rebinding the shot. | `providers.test.mjs` covers the long-wait floor, network loss, 5xx and truncated 2xx; `storyboard-provider-dispatch.test.mjs` covers reservation/trace preservation, retry gating and late cancellation. Full 226/226/architecture/build gate passed. Real v12 trace restored from the batch item's idempotency key as `515372988895616`; no follow-up Provider submission was made. |
| Storyboard video state | Flow bundle `clipsState[clip.id].videoInfo.video_url`, especially around lines 209217-209254 | Each storyboard card owns independent generated video state. | Replace the legacy script-row image group with versioned storyboard clips linked to cinematic shots and approved media. | Storyboard contract and persistence tests. |
| Storyboard-to-timeline import | Flow bundle `batchAddStoryboardVideosToTimeline` around lines 205587 and 209390 | Opens the bottom timeline, skips existing clips, inserts relative to the nearest prior/next storyboard clip, reports added/skipped/failed progress and supports cancellation. | Reproduce behavior; improve dedupe to `mediaId + versionId + checksum`, and persist `StoryboardTimelineImportReceipt`. | Ordered insert, dedupe, partial failure and cancellation tests. |
| Storyboard reference selection | Flow bundle `storyboard-image` state and toggle handlers around lines 203715-203751 and 207374-207397 | Momo exposes an explicit per-card toggle. The user may add the generated storyboard image to the video's reference list, or leave it out; the toggle also updates the visible Prompt marker. | Reproduce the explicit opt-in toggle. Ununu additionally separates semantic composition/action-phase reference from literal `t0`: selected ordinary references enter `image_reference`; only `storyboard_first_frame` with current full-domain pixel proof may set `firstFrameMediaId`. The default is not selected. | `storyboard-v2.test.mjs` and `storyboard-provider-dispatch.test.mjs` assert role mapping and the actual Provider request shape. |
| Storyboard batch production and Provider dispatch | Momo storyboard cards expose per-shot production state and retry/version behavior; Ununu's idempotency and unknown-outcome safety are the industrial addition. | Ordered cards may be retried or cancelled independently while versions remain comparable and timeline import remains stable. | Persist one batch/job/item state machine. In the canonical cinematic path, compile the Prompt Draft, require matching execution node and model preflight, then persist Provider run identity/idempotency before polling; no project budget or UI paid-approval gate. Legacy batch callers may still use the compatibility budget contract. Unknown outcomes reconcile or require explicit abandonment; retry cannot submit twice. A storyboard image is sent as a video first frame only when its explicit per-shot toggle is selected. | `storyboard-batch-production.test.mjs` and `storyboard-provider-dispatch.test.mjs` cover ordering, reuse, partial retry, cancel, version compare, one-submit/one-poll materialization and unknown-outcome safety with fake Providers only. |
| Canvas timeline panel | Lazy module `assets/CanvasTimelinePanel-vit0KCBZ.js` referenced by the Flow bundle around line 209428 | Bottom timeline shares playback state and supports multi-track edit interactions. | Recreate the interaction shell and back it with `TimelineDocumentV2`: persistent tracks/clips, trim/split/ripple/slip/snap, transitions/effects/markers/keyframes, resource history, frame stepping, local preparation and background delivery. The long command row scrolls from its first control instead of creating unreachable negative overflow. | `timeline-v2.test.mjs`, `professional-timeline-operations.test.mjs`, `media-preparation.test.mjs`, `advanced-render-delivery.test.mjs`; 1440×900 Browser persisted one A1 clip, prepared its real waveform/proxy and advanced the shared clock to 1.15s. Evidence: `docs/evidence/screenshots/20260720-professional-timeline-1440x900.png`. |
| Canvas player panel | Lazy module `assets/CanvasPlayerPanel-BFGcrZIa.js` referenced by the Flow bundle around line 209438 | A single player can detach via Teleport, remain above the canvas, drag, resize and re-embed without losing playback state. | Implement one shared player instance; default detached size 400×300, minimum 300×200 and viewport clamping. Docked mode remains in the right rail; detached mode is the only core surface allowed above the canvas and uses z-index 110. | `player-workspace-policy.test.mjs`; Browser verified docked → detached → re-embedded identity, 400×300 detached geometry and computed z-index 110 above the automation window at 104. |
| Player/timeline layout | Player and timeline bundle sources plus the Flow layout around lines 209390-209669 | Right player/inspector and bottom timeline remain part of the same infinite-canvas workspace. | Reproduce the layout; large cinematic editors expand inside the canvas, not in full-screen modal workspaces. | Fixed-viewport visual QA. |
| Background render entry | Momo timeline/player modules establish the timeline as the assembly surface; the extracted bundle does not establish Ununu's professional master/QC contracts. | Timeline actions stay in the bottom dock and do not navigate away. | Explicit Ununu enhancement: idempotent background jobs preserve gaps and render H.264/H.265/ProRes/9:16/1:1/WAV; preview/render share persistent transitions/effects/keyframes/subtitles. QC, ExportMaster, stems, EDL/FCPXML, checksums, delivery manifests and downloads remain local/auditable. | `render-job.test.mjs`, `advanced-render-delivery.test.mjs`, `media-preparation.test.mjs`, full-auto 423 and restart-resume tests use real FFmpeg/FFprobe and verify no duplicate render. |
| Full-auto Agent production flow | Momo's internal Agent/session surfaces establish observable task activity, but Ununu deliberately does not copy the chat UI. The cinematic orchestration contract and its large canvas-level observer are Ununu industrial additions. | Before start, the user can inspect the planned DAG; during execution, the same surface changes into a live projection instead of becoming a black box. The observer is entered from the project left capsule, not nested under the compact top control. | The top control owns only mode, budget and lifecycle actions. A dedicated left-capsule entry opens the canonical 13-stage dependency DAG in a non-modal canvas-level work window whose exact default frame matches `影视总控` (1180×800 at 1536×900). It shows live Agent cards, exact activity, progress, artifacts, elapsed time, budget reservations and failures. Control/worker leases heartbeat, stale work checkpoints/requeues, restart rotates control ownership, and paid tasks reuse one reservation/run identity. Image/video/storyboard/sound dispatch uses the same approval/budget/recovery boundary. The surface stays owner-read-only and adds no AI chat. | `automation-task-dag.test.mjs`, `automation-flow-view-model.test.mjs`, `automation-flow-window-policy.test.mjs`, `automation-recovery.test.mjs`, `automation-executor.test.mjs`, `budget-control.test.mjs`, `project-control-api.test.mjs`; final 219-test/architecture/build gate. Browser verifies the plan/live states, single entry, large window, close/reopen/drag/resize, z-index 104 and detached-player priority at 110. |

## Required entry state model

Each new parity entry must record:

1. Source module and line/chunk.
2. Initial visible and persisted state.
3. Trigger gesture or command.
4. State transition and observable result.
5. Empty/loading/error/cancel behavior.
6. Whether the result is Momo parity or an explicit Ununu cinematic addition.
7. Automated and visual evidence.

Unknown behavior is blocked from implementation until source evidence is found or the behavior is explicitly approved and labelled as an Ununu addition.

## Complete-plan coverage matrix

This is the completion ledger for the original eight-stage rebuild. A stage is `complete` only when its contract, core use cases, persistence, API/CLI boundary, Web interaction, automated tests and fixed-viewport evidence exist. A visually present control is never counted as complete when its durable behavior is missing.

| Stage | Current state | Completion evidence | External boundary |
| --- | --- | --- | --- |
| 0. Source-parity freeze | Complete | Every active Momo-derived surface in this ledger has a local bundle location, persisted/visible state, interaction transition and automated or fixed-viewport evidence. Unknown behavior remains blocked instead of being inferred from screenshots. | None inside the local product. |
| 1. Control state and hard-cut foundation | Complete | Transactional V2 hard cut; backend 423; control/worker heartbeat; stale lease checkpoint/requeue; takeover/pause/cancel; task budget reservation/consumption/release; restart reuse. Covered by migration, project-control, budget and automation-recovery tests. | A real paid run still requires owner approval. |
| 2. Canvas shell and node system | Complete | Sole Momo canvas shell, distinct base nodes, selected-only Prompt/ports, invisible corner resize, fixed rail panels, source-sized World/Audio, Compare/Grid/ImageEdit policies, real SPZ Director and stable large-node collapse. Fixed 1440×900 evidence covers the user-blocking World/Audio/Director states. | World generation remains disabled until a verified adapter exists; import/render/control are complete. |
| 3. Industrial assets and rich Prompt | Complete | `PromptDocumentV1`, atomic reference/skill/constraint tokens, stable bindings and output purposes; scoped authority search/derive/batch/approve/history/restore/impact/compile through Core/API/CLI. | Provider execution follows the common approval gate. |
| 4. Story, shots and storyboard production | Complete | Script planning persists scenes/beats/shots/storyboard; shot/card ordering, references, batches, partial retry/cancel, version compare, safe Provider dispatch and ordered timeline import all use durable receipts. | No real paid Provider was invoked during verification. |
| 5. Shared player and professional timeline | Complete | One player detaches/re-embeds at z110; `TimelineDocumentV2` persists tracks, clips, ripple/slip/snap, transitions/effects/markers/keyframes and histories. Media preparation and the unified playback clock passed browser/restart verification. | None. |
| 6. Edit, sound, render and delivery | Complete | Real FFmpeg/FFprobe fixtures cover preview/render effects, captions, audio mixing/stems, H.264/H.265/ProRes/vertical/square/WAV, QC, EDL/FCPXML, checksums, package manifests and downloads. DCP/IMF remain explicit adapter-only formats, not fake buttons. | A future DCP/IMF adapter is outside this requested local delivery set. |
| 7. Full-auto cinematic production | Complete | Canonical 13-stage DAG, activities, lease recovery, checkpoints, budgets, imported-media completion and one-submit/one-poll paid sound fixture. Image/video/storyboard/sound all share the approval/idempotency boundary; the observer remains non-modal and below the player. | Real paid activation requires owner approval and credentials. |
| 8. Final cutover and performance | Complete | `MomoCanvasWorkbench` is the sole Web path. The 1000-node/2000-clip official-use-case reopen test remains inside the local performance gate. Final `npm run verify` passes 221/221, architecture boundaries and production build. | Historical data was not destructively archived because no explicit owner approval was given. |

## Post-completion capability boundary

There is no remaining implementation gate inside the authorized local scope. The only named unavailable capability is text/image-to-SPZ generation: no verified, approved World Provider is configured, so the World Prompt remains truthfully disabled. Activating a future adapter must reuse the existing Core/API approval, budget, idempotency, unknown-outcome reconciliation and restart-recovery contracts. No legacy workspace or direct Web Provider path may be reintroduced.

## Real production evidence after source-parity cutover

- The Momo-derived canvas remains the only visible workspace while the separately authorized `《血月客栈》` production is executed through the same Core/API/CLI path. Real keyframes, Director captures, GenerationUnits, Provider runs and budget state all project back onto canvas nodes; no shadow database or legacy `ununu-web` runtime is used.
- KF02 v14 (`media-b20660b5-c3e3-480b-ad38-eacb58d6c465`) completed after about 189 seconds and was accepted only after origin-vector, signed screen direction, irreversible creature anatomy, identity and environment-delta gates passed. This is runtime evidence for the restored five-minute-minimum/thirty-minute-default image wait contract.
- P01 uses nine visible, ordered bindings: five accepted authorities/action references, KF01/KF02 accepted keyframes and two shot-specific Director Stage captures. Director aliases are camera-specific, and the opening Prompt is shot-local instead of leaking future Boss, injury, weapon-break or finale beats.
- Verification is now **240/240** tests plus architecture boundaries and the Next.js production build. P01A is accepted; the old P01B candidate is rejected and visibly labelled, while the corrected authoritative-tail P01B remains unpaid until the owner chooses to dispatch it.

## 2026-07-21 Owner veto / defining-identity gate supersession

This section supersedes the earlier ledger statements that P01A remained accepted or that P01B had a ready authoritative tail.

| Surface | Momo source evidence | Verified behavior | Ununu implementation decision | Verification |
| --- | --- | --- | --- | --- |
| Latest evaluation wins | Momo exposes checkpoints and review decisions but does not prove a persisted per-generation-unit override rule. | A later Owner decision must be able to revoke an earlier favorable candidate without deleting its audit history. | Core automation and node projection resolve only the latest evaluation for each `generationUnitId`. `cinematic-evaluation-bloodmoon-p01a-owner-veto-reject-v4` revokes the prior P01A ACCEPT and blocks every dependent continuity source. | `cinematic-continuity-policy.test.mjs`, `cinematic-evaluation-node-policy.test.mjs`; live P01A/P01B canvas replay. |
| Defining visible fact veto | Momo review scores do not establish that a high aggregate score may override a failed creature identity fact. | The sole complete face must be embedded in rear occipital skin; the same head's front is smooth skin with no eyes, nose or mouth. Exposed skull, ordinary front face, two faces or a second head are different creatures. | `GenerationUnit.reviewRequirements` and `Evaluation.visibleEntityChecks` are persisted. Every blocking fact must pass and `vetoFindings` must be empty before ACCEPT, regardless of aggregate score. | `cinematic-evaluation-gate-policy.test.mjs` including the real HTTP evaluation endpoint; focused 16/16. |
| Authority pixels over authority prose | Reusable asset cards do not prove that an accepted thumbnail actually satisfies its textual identity description. | Authority/KF media must be reviewed at original-resolution pixels before it can control downstream generation. Every required orthogonal view must independently pass; plausible front/back panels cannot excuse a reversed side view. | Old authority/KF media remain rejected. r7 candidate `media-a001356f-f97b-4813-afae-9968f32e0532` was also rejected because both side views restore a normal body-front face and create a blank rear bulge/second head block. Authority is now r8 `rejected`; current asset version is `audit_only` and prohibited as authority/state carrier/reference/frame anchor. | Latest media review `review-df5a8378-513d-4477-9494-0e0b88e01ba5`; asset version `asset-version-9a3c19ac-5965-4a2c-b399-4628b29b949d`; live CLI canvas/preflight verification. |
| Revision-current knowledge signoff | Momo 的 Agent/activity 表面能证明有角色参与，但不能证明旧 production 级意见已覆盖当前镜头 revision，也不能证明用户提供的电影知识已进入执行合同。 | 只有精确覆盖当前 Shot/GenerationUnit、引用 `cap-* + kn-*`、绑定当前 TeamManifest 并改变可验收合同字段的贡献才是付费会签。 | Ununu 新增知识到逐镜的确定性门禁；production 模式的正式角色单元自动拒绝空 TeamManifest、过期 target、纯文档引用和 manifest mismatch。 | `cinematic-compilation-context.test.mjs`、`cinematic-prompt-policy.test.mjs`、`cinematic-compilation-staleness.test.mjs` 与 HTTP API integration；真实 P01A compilation `prompt-compilation-531072fb-33ee-4e8e-babc-92aa5358a334`；最终 263/263。 |
| Director proxy responsibility isolation | Momo Director references establish composition/blocking but proxy figures are not creature-identity masters. | A spatial plate may prove entrance foreground, table/seat occupancy and camera axis while proving nothing about rear-face anatomy. | Rebuilt S01 capture `media-fa00b807-15bb-45fd-aba3-8e75866e53b1` controls spatial relationships only; `doesNotControl` explicitly includes identity, anatomy and final style. | Director stage revision 159/capture lineage and direct 1280×720 pixel inspection. |
| Safe unpaid recovery boundary | Momo's batch workflows can pause at confirmation, but do not prove Ununu's Provider-specific authority/preflight chain. | Rebuilding a rejected shot must not silently reuse bad pixels or dispatch a paid retry. | P01A is `FIRST_FRAME` with no first frame/reference until a later corpse identity master and new KF01 both pass exact pixel review. The failed r7 authority is r8 REJECT/audit-only. P01B keeps v4 REJECT as its continuity source and stays blocked. | P01A compilation `prompt-compilation-e459dfc0-4f84-4cc8-a47e-062a7af3f3c7`; P01B compilation `prompt-compilation-21b21b2b-aaba-4c7b-8ee7-148f5f532124`; both `ready:false / stale:false`, no Provider call. |

Current verification is **258/258** tests plus architecture boundaries and the Next.js production build.

## 2026-07-21 cross-modal and overlap-handoff parity update

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Per-shot I2V visual-state carrier | Momo/外部工作流能传图片，但不能证明同一张泛化故事板可保持多个镜头的身份、拓扑、站位、构图和连续性。 | 每镜用当前 Shot revision 的真实关键帧，把五域状态压进像素；最新 media ACCEPT、checksum 和 pixel review 缺一不可。 | `acceptanceProof` + Core `visualStateCarrierAudit`；后写 media REJECT 使 compilation stale；frame-input 禁止普通参考时只送实际首/尾帧，其余 lineage 留在 sourceVersions。真实旧 KF01 已由最新 `review-04db9509-5eee-465a-a18c-4ead0c808ed4` 覆盖旧 ACCEPT，并同步为 StoryboardShot r108 / Shot r39、`needs_regeneration`。 | `cinematic-cross-modal-control-policy.test.mjs`、`cinematic-compilation-staleness.test.mjs`、`cinematic-contracts.test.mjs`。 |
| Tested overlap handoff | 已有本地实测证明同条件下显式 H0→H1 复演产生可剪重复区，并在 H1 后继续；未证明跨模型万能触发。 | `TAIL_CONTINUE` / `DUPLICATE_HANDOFF` 二选一；后者 H0/H1 同源于最新 ACCEPT、帧不同、H1 后有新内容、剪掉重复区、无固定秒数。 | `ContinuationHandoff`、来源 evaluation staleness、Prompt `【续镜交接】`、五项守恒和 motion/camera/lens/audio verification gates。能力保持 `LIMITED`。 | `cap-overlap-handoff`、`cap-multi-video-overlap-handoff-rule`、三个 `kn-*`；Core/Prompt/HTTP integration。 |
| P01A→P01B hidden seam | P01A 是主角后方机位，P01B Director camera 是白璃前侧 30°；无真实遮挡时不能谎称直接无缝。 | 只有新版 P01A H1 像素出现白璃斗篷/门框全画幅遮挡，才允许 foreground-wipe 隐藏机位重置；否则阻断。 | P01A Shot/Unit revision 39 已把“后脑唯一完整人脸清晰保持至少 0.8 秒后，斗篷左下→右上全画幅擦镜”写入时间线和第 7 条 review requirement；P01B revision 11 已持久化 plan，但仍无真实 H1/reference，来源仍为 Owner v4 REJECT。 | P01A `prompt-compilation-a58c0bbf-8e56-496e-b152-e07ad350ab64`；P01B `prompt-compilation-385ef5bb-abe1-43e9-9648-f02c12b15983`；均 `ready:false / stale:false`，无 Provider call。 |
| Constraint-release hypothesis | 用户截图展示“首帧后调角度/拉景别使模型逃逸”，但没有模型版本、seed、完整成片和失败组，不能作为通用实证。 | 把文生/I2V/首尾帧视为自由度与连续性之间的约束预算；任何 release 只释放明确的摄影构图字段，定义性身份/解剖/数量/接触/站位不得一起漂移。 | Skill 与主路径文档要求文生、首帧、首帧＋release、相容首尾帧四组模型匹配 A/B；P01A 在尸傀解剖证明前禁止 release，只能在真实全画幅斗篷遮挡后重置机位。 | 用户参考图仅登记为 hypothesis；以全时间线身份/拓扑/blocking/非预期切镜/伪影/可剪性决定是否升级能力。 |
| External-run loading at overview zoom | Momo 源码没有证明外部 CLI 发起时在 20% 缩放必须如何呈现资产 loading；这是 Ununu 本地控制面的新增可观察性要求。 | CLI/API/自动化运行必须在画布上低缩放可见，终态刷新媒体；新候选不得继承旧候选 rejection。 | `CanvasNodeCard` 为所有 running 节点增加 overview 可见脉冲和标题状态，资产内部仍显示生成消息；authority 完成态重置当前 review 为 candidate，旧 rejected media 保留历史。 | 真实节点模拟 running 后 DOM 同时出现三层 loading，随后 CLI 恢复成功媒体；focused 8/8、architecture/build 通过。 |

## 2026-07-22 Current-revision veto 与 Provider 控制图隔离

| Capability | Source parity / gap | Industrial rule | Ununu implementation | Evidence |
| --- | --- | --- | --- | --- |
| Vetoed specialist contribution | Momo 的角色活动记录不能证明该角色已批准当前镜头，更不能让一份含 hard veto 的贡献反向满足 signoff。 | `vetoFindings` 非空即 REJECT；target、知识、角色和 Manifest 即使全匹配也不得计作会签。 | `cinematic-professional-signoff-policy.mjs` 的 current/grounded/manifest 三层资格全部要求 no-veto，并输出 `vetoedContributionIdsByRole`。 | 四份 P01A r50 veto contribution；Core + HTTP regression；focused 18/18。 |
| Editor-only Director evidence | Momo/Director 控制图可辅助空间规划，但中文标签、图例、箭头和粗代理像素不能混入成片参考。 | `editor_only` 只进入审计 lineage；最终 Provider refs 必须排除。 | compiler 为该 binding 标记 `providerEligible:false`，Provider selection fail-closed 过滤；sourceVersions 仍保留 capture lineage。 | r51 compilation 恰好六 refs，排除 `media-0dbc640d-7817-437a-8353-3fda67b2ba5c`；回归测试通过。 |
| Physically observable identity proof | 任何参考项目都不能让一个入口背视机位同时看见同一不透明头颅的前后表面。 | 当前镜头只审真实可见证据；画外定义性身份规则保留为 Authority invariant，并在它入画时一票否决。 | Shot r47 / Unit r51 / Prompt coverage / reviewRequirements 统一 rear-face + single-head + body/table proof；front-faceless 为 conditional pixel veto。 | compilation `prompt-compilation-39049321-509d-4de1-b6e7-cdf20e6fa0d6` technical preflight true。 |
| Scene objective propagation | 上游剧情正确不能保证 Provider Prompt 没有丢失人物为什么进入场景。 | 主动任务必须贯穿 Story→Shot→Unit→compiled Prompt；退出/撤退/提前攻击语义显式禁止。 | P01A r47/r51 将“为逐一消灭怪物主动进入”写入 narrative、performance、temporal phases 和 final Prompt。 | 编剧 ExpertPack `ep-387eec1a1664dbab19d0` 同版本复审 PASS；Owner/Manifest gate 仍独立。 |
| Hybrid image-text semantic control | Momo rich reference slots prove stable media binding, but not that every visible pixel in a reference has equal authority or that a still contains motion. Owner supplied the concrete counterexample: keep people/blocking, replace a modern table, complete occluded corpses, and drive motion with text. | Separate four consistency targets; distinguish semantic `image_reference` from literal `first_frame`; declare per-image preserve/replace/complete/ignore/style/temporal role and a separate dynamic contract. An actual first frame only owns the `t0` entry/continuity boundary; `t0+1` onward remains dynamic. | `cinematic-generation-control-policy.mjs`, `ReferenceBinding.semanticControl`, `GenerationUnit.controlIntent`, compiler 2.9.1 `【参考图语义职责】/【独立动态合同】`, `t0_boundary_only` preflight policy and Canvas mode/control summary. | New policy/contract/prompt/API tests; P01A r41 and P01B r12 mode-control audit pass while paid readiness remains blocked. |

Current full `npm run verify` is **283/283** tests, with architecture-boundary checks and the Next.js production build passing. The asset-card REJECT projection and historical-preview isolation are included; this line supersedes the earlier numeric verification line.

## 2026-07-21 Prompt 完整性与通用运镜控制增量

| Capability | Required behavior | Implementation | Current evidence |
| --- | --- | --- | --- |
| Prompt domain closure | 图片静态 12 域、视频额外动态 8 域必须逐项可见可验收；失败反例必须回写漏项、正向约束和否决标准。 | `cinematic-prompt-coverage-policy.mjs`，图片/视频 compiler 与 production preflight。 | 尸傀 r9 的“普通侧脸＋第二头块”失败已形成 counterexample closure；未自动重试。 |
| Structured camera motion | 推拉、跟移、升降、摇臂、摇/俯仰、变焦/拉焦、手持、环绕或复合运镜必须有数值起终状态与匹配控制图形。 | `cinematic-camera-trajectory-policy.mjs` + `cameraTrajectoryPlan` + Shot/Prompt/preflight。 | 缺计划被阻断；path/orientation/lens/envelope/compound 类型受合同约束。 |
| Mode-aware camera annotations | 路径线、视轴弧、镜头曲线、手持包络、圈选和箭头可帮助模型理解局部与运镜，但不能成为真实首/尾帧或成片物体。 | `editor_only` 永不进 Provider；`provider_reference_only` 只允许独立派生标注图进入 `image_reference`。图和 Prompt 共用 `controlGeometryId`、标注 ID、含义与时间窗；首帧/首尾帧、几何冲突、超时或缺绑定均阻断。 | `cinematic-camera-trajectory-policy.mjs`、Schema/Web types 与 Prompt/Provider focused tests；保留干净首/中/尾 capture。 |

## 2026-07-21 temporal-motion and optional-storyboard parity update

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Optional storyboard | Momo 有故事板与批处理，但不能证明每个镜头都必须生成故事板，也不能证明静态 panel 包含时间演化。 | 只在构图、调度、局部定位、动作/VFX phase 或切镜审批需要时启用；普通 composition/action panel 只能是 semantic reference，不自动成为首帧。 | 故事板自动首帧提升已移除；P01A r44 不再有 keyframe gate，采用 Director semantic `image_reference`。 | `storyboard-v2.test.mjs`、`storyboard-provider-dispatch.test.mjs`；P01A compilation `prompt-compilation-5f27e5e4-8bbb-475e-b7c4-a88dfe2b2e1a`。 |
| Adjacent-frame temporal motion | 外部项目/技巧资料可描述镜头和时段，但不能证明仅凭静态图或自然语言按秒区间就能保证动画连续。 | 用 timed animatic/Director previs 验证运动，并把阶段、逐对象状态轨、相邻状态过渡、速度/接触/中间态和逐帧派生量验收写成可机检合同。 | 新 `cinematic-temporal-motion-policy.mjs`、`temporalMotionPlan`、compiler 3.3.0 `【逐帧时空运动】`、`requireTemporalMotionPlan` 付费门禁。P01A r44/P01B r14 都因真实计划尚缺而 blocked。 | temporal/Prompt/API focused 39/39；完整 300/300、architecture/build；两单元 `ready:false / stale:false`，无 Provider call。 |

## 2026-07-21 Director World review closure

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Rejected world cannot remain active | Momo/Director 能显示连接的世界，但不能证明候选或已否决像素可进入电影主生产路径。Owner 截图显示旧红色全景虽标“非权威”却仍真实铺在导演台视口。 | 世界主体和预览图必须 latest media review=`accepted`；后写 REJECT 需撤下当前环境、降级依赖 Authority，并隔离在错误环境上产生的 shot capture。 | `director-world-environment-review-policy.mjs` 同时门禁 `bind-world` 和直接 `set_environment`。真实错误媒体最新 REJECT、Scene Authority r7 rejected、Director v161 无 environment；P01A/P01B Shot r41/r36 无 Director binding，新 compilation 无 Director refs。 | focused 9/9；完整 302/302、architecture/build；Browser 刷新显示“全景参考 · 未绑定 3D”和纯3D调度几何。无 Provider 调用。 |

## 2026-07-21 Story/Shot Owner revision gate parity update

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Story before Shot before visual production | Momo/开源项目可生成 planning/shot DAG，但不能证明上游剧情或分镜错误时会自动停止后续视觉浪费。 | 当前剧情 revision 必须先由 Owner 接受；随后完整有序分镜脚本逐镜接受；旧 revision ACCEPT 和高分不得授权当前版本。 | 公共 review target contract + Core `cinematic-story-shot-owner-review-policy.mjs`；GenerationUnit、AssetAuthority 付费图片与 Storyboard 付费图片/视频在预算 reserve 前共用 deterministic gate；sourceVersions 记录 latest verdict/staleness。 | Pure policy、staleness、Core、HTTP API、automation、Authority 与 Storyboard integration；310/310、architecture/build。 |
| Real Bloodmoon audit stays blocked | 当前 Story r2 仍写“酒客统一转向入口”，与 Owner 锁定的身体朝桌/头前无脸/后脑唯一脸相冲突。 | 不接受错误 Story；P01A/P01B 不得因已有视觉规划而越过剧情与分镜审查。 | Story r2、P01A Shot r41、P01B Shot r36 均无伪造 ACCEPT；两 unit compile 记录 exact null review lineage 并报新错误码。 | `prompt-compilation-6616f8d0-132b-420f-9d80-b667e9779ca3`、`prompt-compilation-fd3ab818-e7d6-4f47-9407-743da3108ba9` 均 `ready:false`；P01B v4 REJECT 连续性门仍在；无 Provider。 |

## 2026-07-21 Causal acting timeline and Bloodmoon Story r3 update

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Acting is a causal time process | Owner 的离婚固定长镜头 Prompt 可证明“平静 → 外部离场刺激 → 强撑微反应 → 阈值 → 崩塌”的写法比单一情绪标签完整；技巧图的浅景深与皮肤解剖不能单独证明表演层次。 | 每个 Shot 明确初态、触发、连续秒级节拍、内在判断、可见证据、阈值、终态与禁止提前泄露；摄影焦点和可见解剖细节按景别另行约束。 | `cinematic-performance-timeline-policy.mjs` + `cinematic-revision-review-use-case.mjs` + Prompt renderer；Owner ACCEPT 前阻断，视觉生产复核再次阻断。 | Generic-label、gap/overlap/duration、render、Core owner review、HTTP/Authority/Storyboard/automation focused 30/30；完整 315/315、architecture/build、Skill validation。 |
| Bloodmoon upstream correction | Story r2 和旧 Shot 套话不能继续代表当前 Owner 意图。 | 先修 Story，再逐镜把动作、表演、不可逆道具与接缝写成时间因果；仍需 Owner 审查，不自动接受。 | Story r3；14 个 Shot 各有 4–6 个节拍；P01A Shot r42；P01B Shot r39、Unit r15/5秒/8项硬检查；斗篷 H1 后先守恒原桌席，再可见起身、对白、三符、倒地和顾沉启动。 | P01A `prompt-compilation-97ec4082-8e83-4393-a50b-7ef8e83cdc03`、P01B `prompt-compilation-061f77e5-9ed2-4330-9cba-3ba9d9689e5f` 均记录 Story r3 和 null Owner reviews；P01B v4 REJECT 与真实 H1/连续性门保持，无 Provider。 |

## 2026-07-21 Movement-motive and screen-direction parity update

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Mission motive before blocking | Owner 明确三人主动进入是为剿灭怪物；不能用“前门退路被封”补造行动理由，也不能把“杀出去”解释成逃离客栈。 | Story 先区分 assault/pursuit/rescue/escape/retreat，再把命令绑定命名空间轴、目标与向量；Shot/Unit 同时锁髋肩、领先脚、眼线、武器、VFX 与摄影方向。 | Story r4；P01B Shot r40；Unit r16；blocking review requirement `p01b-extermination-direction`；Skill 已增加通用 movement-motive gate。 | P01A `prompt-compilation-f161aacd-c2b6-4d1f-b8f8-47ba302d0e88`、P01B `prompt-compilation-5a14bac8-517e-4aa9-b253-ce9d608732fa` 均 `ready:false`；P01B v4 REJECT/H1/Owner gates 保持，首帧与 refs 为空，time mismatch 不存在，无 Provider。 |

验证：Skill quick validation；`npm run verify` 315/315、architecture boundaries、Next.js production build。

## 2026-07-22 Story r7 acceptance-scope and silent-targeting parity update

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Revision-scoped short Owner confirmation | Owner 在明确 Story r7 审查问题后回复“可以”；该短答本身不携带对下游工件或付费执行的授权。 | 把确认只绑定到紧邻问题中命名的 artifact/revision；Shot、资产、关键帧、GenerationUnit 和 Provider 各自需要独立决定。 | Story r7 review `review-e65a5c46-94c0-4334-9d76-294af6651332` accepted；Shot r43/r41 review 仍为 null。Skill 已固化严格作用域规则。 | P01A/P01B compilation `ownerStoryShotReviews` 均显示 Story accepted、当前 Shot accepted=false；无 Provider。 |
| Deleted dialogue invalidates the whole downstream chain | 旧 r40/r16 虽用 intent 重新解释对白，仍把错误字面前提扩散到表演、声音、时段、续接和验收。 | Story 删除对白后必须同步删除下游口型、发令、对白声桥、命令状态和运动任务；不得只改台词字段。 | Shot 2 r41 与 Unit r18 改为无对白 A/B/C 目标锁定、三符/三轨/三命中/三倒地，再由顾沉进入当前交战面；H1 只定义 t0。 | P01B compile `prompt-compilation-c837b822-e3a2-4cb5-bed0-9151cf68fbfd`；递归旧语义扫描为零，performance/coverage 通过，Shot gate 和 P01A v4 REJECT 保持。 |

当前阶段没有新增产品源码改动或 Provider 调用；真实 CLI revision/compile lineage 已验证。当前全量复跑为 314/315，唯一失败是高系统负载下 2000 clip 写入 5164.8ms 超过 5000ms 性能阈值；上一轮 315/315 保留为最近干净基线，不把本轮记为全绿。

## 2026-07-22 Shot-local execution and rejected-capture parity update

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Explicit approval remains artifact-local | Owner 的“可以”紧跟明确列出的 Shot 1 r43 / Shot 2 r41，不包含后续视觉或付费含义。 | 每个命名 revision 独立持久化；资产、关键帧、Unit 与 Provider 不随 Shot ACCEPT 自动放行。 | Shot 1 review `review-0eae0b11-07b1-4c0a-85e9-5a3a40aa3489`、Shot 2 review `review-ab974302-781d-47ac-9e41-f310018e4f67`；P01A/P01B 新 compilation 仍 `paidReady:false`。 | CLI review/compile lineage；首帧和 refs 为空，P01B v4 REJECT 保持。 |
| One Shot carries only its own five-second world | 外部 Prompt 模板常把项目级声音、灯光和动作词复制到每镜，不能证明模型会自行忽略未来事件。 | 当前 Shot 只包含本镜动作、声音、光效、受力与连续性；未来事实只能作为简短 negative lock。 | Shot 3 r10 删除断枪、破墙、腕骨和雷击的正向声音/VFX，六个连续时隙覆盖 0–5 秒，并绑定同一组表演节拍与接触链。 | CLI schema save 成功；时隙/表演端点一致，source row v3 / document r32。 |
| Rejected Director lineage is cleared recursively | 一个顶层绑定为空并不能证明嵌套 binding、copied camera 或世界媒体没有继续进入 Prompt。 | 世界 REJECT 后清除当前 Shot 的顶层、blocking 嵌套和 copied camera；历史 capture 只留审计。 | Shot 3 r10 三处均为空，旧 Director node/capture/image/world media 四个精确 ID 递归扫描为零。Skill 同步明确递归清理规则。 | 真实 Shot list 回读；无 Provider。 |

当前 Shot 3 r10 仅为待审候选，尚无 Owner ACCEPT。没有重新运行产品全量 verify；最近一次结果仍为 314/315，唯一未闭合项是高负载下的 2000 clip 性能阈值。

## 2026-07-22 Full ordered-shot audit parity update

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Source rows cannot contradict accepted Story | 外部脚本/分镜工具通常把原句直接传播到图像和视频 Prompt，不能自动知道“眉心”与已锁定后脑唯一脸、预埋符与可见动作源、屋顶雷柱与掌心因果之间的冲突。 | 在逐镜修订前先将结构化 source row 对齐 latest accepted Story，同时保留原 `sourceExcerpt` 作为审计证据。 | Shot 5/7/13/14 source rows 升到 v3，document r36；Owner anatomy/action normalization 独立保存。 | API 回读 row version/document revision；Story r7 locked facts 对照。 |
| Ordered Shots are one state machine | Momo 能维护分镜顺序，但不能证明肢体占用、道具换手、伤势功能、尸体/焦痕和未完成动作在 10 个后续镜头中守恒。 | 每个 Shot 的 opening/ending、hands/props/contact、performance、time slots 与 edit handoff 必须逐边界闭合；任何状态变化需可见过程。 | Shot 5–14 分别修为 r35/r9/r9/r13/r35/r35/r9/r35/r35/r35；刀鞘归位、断枪两截、骨折换手、直刀拔出、腿锁和 Shot 14 换手均显式时序化。 | 14-Shot CLI list；slot boundary validator；锁定对白/道具/伤势递归扫描。 |
| Rejected visual lineage never survives a batch rewrite | 旧 Shot 5–14 同时含顶层 Director binding、blocking 嵌套 binding、copied camera 和 rejected world media；清一个字段不足以阻止旧像素再次成为参考。 | 全部当前引用递归清空，历史 capture 只留审计；每镜要求在合格 Authority 像素上重建 Director、干净参考图和关键帧。 | 十镜 top binding 均为 `null`，嵌套/copied camera 和旧 Director/world 精确 ID 为零。 | 实际 Shot payload 递归扫描；无 GenerationUnit preflight、无 Provider。 |
| Audit permission is not batch acceptance | Owner 要求“一次性审核到底”，但未逐镜确认 Shot 5–14 最终 revision。 | 可一次性诊断和修复；只有命名 artifact/revision 的后续明确决定才能写 ACCEPT。旧 Unit 只允许安全 compile 证明阻断，不得借审核权限进入付费。 | Shot 4 r30 仅由 `review-18e80471-ebfe-4fcf-ae78-5ab4f860b929` 接受；Shot 5–14 全部保持待审。Unit r03–r08 compile 均 lint/preflight false；r04/r05 另暴露旧 unit leak/time mismatch，等待 Shot 接受后重建。 | Review lineage + current revision list + 六个 compilation gate。 |

验证：focused evaluation gate 6/6；完整 `npm run verify` 315/315、architecture boundaries、Next.js production build；无付费 Provider 调用。

## 2026-07-22 Authority pixel and GenerationUnit lifecycle parity update

| Capability | Source/evidence boundary | Required Ununu behavior | Current implementation | Verification |
| --- | --- | --- | --- | --- |
| Consolidated review may become exact revision acceptance | Owner 在完整 Shot 5–14 审查之后明确说“接受 继续”，但没有接受任何视觉资产或付费动作。 | 将决定精确写入十个当前 Shot revision；不扩张到 Authority、KF、Director、Unit 或 Provider。 | Shot 5–14 的十个独立 ACCEPT 已持久化；Story r7 与 Shot 1–14 现均有 revision-current Owner lineage。 | Review IDs 以 `review-24c1332e-...` 至 `review-a81b6561-...` 独立保存；Provider 0。 |
| Authority prose cannot override pixels | 资产卡、六视图排版和 accepted 文本都不能证明侧视方向、同一头颅解剖或空间拓扑正确。 | 原分辨率逐视图审查；任一侧视恢复普通前脸、第二头块/卵囊，或场景后轴敞向室外，都否决整份媒体与全部派生。 | 尸傀 4/4、场景 5/5 旧媒体 REJECT；Authority 分别回到 r13/r8 candidate，accepted version 为空。 | 媒体 reviews、asset-version audit-only lineage、KF02 latest REJECT 与画布节点状态。 |
| Rejected world invalidates every capture | 旧 Director stage 的代理几何本身可能合理，但其可见环境来自已拒绝场景像素。 | 几何仅可作坐标草稿；World/environment、capture 和关键帧参考全部失效，重新绑定 accepted scene 后再捕获。 | World/Director active media 清空，14 captures 为 `stale_invalid_authority_lineage`，KF01–14 不再持有 active reference。 | CLI node/storyboard 回读；Provider payload refs 为空。 |
| Unit lifecycle is an execution gate | 旧记录只有“待重建/blocked”标签，不能证明误触正式运行不会付费；旧 UI 还可能把失效节点显示成“等待生成”或继续展示旧图。 | `active` 外的 lifecycle 必须在 compile/preflight 失败；`superseded` 带原因和替代计划；run 在预算和 Provider 前中止；非 active 状态必须同步到节点空态，失效媒体只能留在 `stale*` 审计字段。 | P01A/P01B 分别 blocked；P02–P08 superseded。新增 policy、schema、Web type、compiler hash、Core node projection 与诚实 empty-state UI；14 张 Director capture、KF01/KF02 均清除 active media。 | 真实 P02 `generation_unit_superseded`、P01A `generation_unit_lifecycle_blocked`；Browser 回放看到“旧生成单元已废弃/图片已隔离”；focused 134/134 + projection 9/9；完整 320/320。 |

无付费 Provider 调用。下一次任何 Authority 生成仍需新的明确付费批准。

## 2026-07-22 Scene Authority exact-media acceptance update

| Capability | Gap in source/previous state | Ununu production rule | Real project closure | Evidence |
| --- | --- | --- | --- | --- |
| Exact-media Authority promotion | A candidate thumbnail, Authority status flip or short Owner reply alone cannot prove which pixels became reusable, and must not silently authorize downstream paid work. | One transaction lineage must bind latest media ACCEPT + checksum/pixels + accepted asset version + new accepted Authority revision + visible node projection. Recompile dependents and keep every unrelated gate independent. | Scene media `media-28707daf-54bd-4711-acc1-7f2c2b7aef45` accepted as Scene Authority r9. Old five scene versions stay `audit_only`; corpse Authority r13 remains candidate. P01A/P01B remain blocked. | Review `review-4ec6b53a-8b2e-4d29-91cf-e9f53af463bb`; accepted version `asset-version-c0465e41-fada-4299-ba8b-fee191c118b4`; node r37; compilations `prompt-compilation-dbfba27c-1195-4671-8a69-bc914d0da579` / `prompt-compilation-3278c1b5-c014-465a-a2dc-fc2e5d15d4ae`. |
| Provider size normalization is not automatic failure | Requested dimensions and returned dimensions are not always pixel-identical; treating every mismatch as a defect creates false QA failures. | Compare with the supported longest-edge/tier/aspect policy and actual visible detail. Record normalization separately from creative/pixel acceptance. | 1536×1024 request returned 1774×887; it remains below the Owner-confirmed 2K longest-edge cap and is treated as normal delivery normalization, not a technical warning. | Exact checksum `4a1dc94894ab243d6c3f56360f0ed582e2abe3402575cd8679a42fbb179af3fd`; node `providerResolutionPolicy.technicalWarning=false`. |

No Provider request or new cost occurred during acceptance. Budget remains CNY 28 consumed / 0 reserved. No source code changed, so this update does not claim a new full-suite test count.

## 2026-07-22 P01A metric-previs and temporal-contract update

| Capability | Gap in source/previous state | Ununu production rule | Real project closure | Evidence |
| --- | --- | --- | --- | --- |
| Nonmetric appearance versus metric stage | A polished 2D scene image does not prove physical dimensions, subject coordinates or camera travel. | Accepted appearance media controls look only; structured World/Stage owns metric space, occupancy and camera transforms. | Scene Authority r9 remains accepted, but P01A planning uses stage revision 179 and a 20×20×8 m semantic geometry contract. | Scene media `media-28707daf-54bd-4711-acc1-7f2c2b7aef45`; Director node r30. |
| Three-state camera proof | One still or prose camera label cannot prove a smooth 4-second path, a hold or a wipe exit. | Bind start/mid/end captures, transforms, FOV and exact time windows into one camera trajectory; reject orbit, axis crossing and hidden cuts unless explicitly planned. | P01A Shot r46 binds start/mid/end captures and exact 0/1/2.4/3.3/3.7/4-second transitions. | Capture IDs `director-capture-15e3ea91-a237-4401-aef8-571e9f709e27`, `director-capture-e1f3b432-6b8f-4ebd-9de0-ea48c85649f7`, `director-capture-d36050bd-9bb6-4ec8-aadc-7ca0152c2f3c`. |
| Declared intentional foreground crop | A full-frame validator previously treated a legitimate OTS shoulder or cloak wipe as missing-subject evidence, while an inferred exception could hide real omissions. | A camera may crop only exact declared foreground IDs; every background/undeclared subject stays mandatory; invalid declarations fail closed. | New contract/policy/Web/Core behavior persists `intentionalForegroundCropIds` and rejects duplicates through the real HTTP API. | `director-stage-capture-policy.test.mjs`; `director-stage-foreground-crop-api.test.mjs`; focused 4/4. |
| Adjacent-state temporal causality | Per-second prose and key poses alone do not prove paths, velocity, contact change or in-between motion. | Every adjacent state of every moving entity declares path, interpolation, velocity, contact evolution and a required intermediate state; derive motion/acceleration/contact/occlusion checks at 24fps. | P01A Unit r47 carries five phases and 13 tracks in `timeline-p01a-occipital-reveal-r9-v1`; temporal/camera/coverage audits pass. | Compilation `prompt-compilation-445e639c-61d5-47b1-ac01-fe41fab8d133`; focused policy suite 50/50. |
| Geometry proof after repeated identity escape | Rejected rear-face candidates repeatedly convert the intended anatomy into skulls, ordinary faces or a second head despite longer Prompt wording. | Remove every rejected pixel from current references and solve a single-skull coordinate/topology proof before another paid candidate. | Corpse Authority r14 has no references and carries `bloodmoon-corpse-occipital-geometry-v2`; no Provider call. | Authority/node revisions 14/39; five-view control geometry and six veto checks. |
| Technical proof never widens Owner approval | Valid camera and motion contracts do not make a newly revised Shot creatively accepted. | Owner acceptance is exact-revision scoped; a new Shot revision reopens the review gate even when all technical audits pass. | P01A Shot r46 remains Owner-pending; P01B remains blocked by P01A v4 REJECT. | Current preflight `ready:false / stale:false`; no frame/reference on P01B. |

Post-documentation `npm run verify` passed 323/323 tests, architecture boundaries and the Next.js production build. `cinematic-production-use-cases.mjs` remains 499 lines. No paid Provider was called during this update.

## 2026-07-22 P01A Shot r46 approval update

| Capability | Required scope rule | Real project result | Evidence |
| --- | --- | --- | --- |
| Exact-revision Owner approval after technical audit | Owner approval may accept the current Shot contract after camera/temporal review, but must not imply asset, pixel, Unit or paid approval. | P01A Shot r46 is accepted; corpse Authority r14 and Unit r47 remain blocked, and P01B still has no accepted H1. | Review `review-43276f84-bdc6-4318-a979-409d6044dd78`; compilation `prompt-compilation-c4a185d8-3c5c-43b0-bdf8-2a811a068da7`; P01A node r98. |
| Approval removes only its own gate | A valid Shot ACCEPT should disappear from current lint without weakening independent identity, knowledge, Manifest or lifecycle gates. | Story/Shot reviews pass; camera/temporal/mode/coverage pass; remaining errors are corpse Authority, current professional knowledge/Manifest signoff and `generation_unit_lifecycle_blocked`. | Fresh CLI preflight `stale:false / ready:false`; no Provider call. |

## 尸傀 r15 一次性付费候选谱系（2026-07-22）

- 授权边界：仅一张尸傀 `side-anatomy-proof`，CNY 2，禁止重试、视频和自动 ACCEPT。
- 合同谱系：Authority r15 → `image-prompt-compilation-af4b6881-7ee5-4946-b92c-578280c8a466` → `run-a623a855-ed90-44ad-8b83-d0018375378d` → `media-32fde63e-444f-43ac-8369-e9c553c2df00` → SHA-256 `f628fe5c8408961824d2cc437c3faf9e48766db813a2f8756e48e7a8a2aef05b`。请求与实际均为 1024×1536；参考媒体和 reference binding 都为空。
- 像素结论：正常反向侧脸、独立下颌/颈部、巨大空白头囊、头颅超长与偏置颈部继续出现；`review-3f4d43b6-d4a7-4102-9572-7cfc8b847bf6` 覆盖候选态为 REJECT，`asset-version-02bed4fa-6e3f-46ac-958e-2de0a1dcc23d` 只能审计。
- 下游谱系：Authority r16 保持 candidate；P01A Unit r48 / `prompt-compilation-30264b76-b314-4792-b41b-3ab6f5b99329` 仍 `ready:false / stale:false`；P01B 仍指向 `cinematic-evaluation-bloodmoon-p01a-owner-veto-reject-v4`，无 H1、首帧或普通参考。
- 技术结论：这不是“再补几个形容词”即可关闭的缺口，而是同一文本控制模态无法稳定约束反常头颅拓扑。下一候选必须先提供与 Prompt 无冲突的可审计结构控制证据，且需要新的付费批准。

final result: source-to-compilation-to-run-to-media-to-review lineage is complete；the failed pixels are quarantined and cannot leak downstream

## 群体 Authority 与分职参考补账（2026-07-22）

| Capability | Source gap / observed failure | Required industrial behavior | Real project result | Evidence |
| --- | --- | --- | --- | --- |
| Board-local Authority constraints | Rich reference boards do not prove that a single-subject exclusion is valid for a later ensemble expansion board. | Inherit only universal identity/anatomy/medium/topology constraints; compare each inherited exclusion with the board `subjectMode`, purpose and required count. Contradictory no-crowd/no-other-person rules fail before payment. | Corpse Authority r26 compiled the four-pair `ensemble` board after removing the global single-subject conflict. | `authority-board-constraint-scope-policy.mjs`; focused image-prompt regression; full verify 326/326. |
| Accepted group identity is a narrow semantic role | A multi-character board can stabilize variation and shared anatomy, but does not prove t0, action, scene or metric blocking. | Accept exact group pixels separately from the single master and restrict downstream use to `character_group_identity/provider_reference`. | Corpse Authority r27 binds group media `media-711c3702-2b0a-4dcd-9689-517e50896882`; first/last/action/scene/blocking roles are prohibited. | Review `review-2c501973-8aa0-4c31-9e0c-c2ec15f5e4ec`; asset version `asset-version-aa1c1d12-0f18-493b-8dff-a3b96f7016c5`; checksum `1ff5d4230ff9a6cf5f306313a9f0e1bb384b20b5ddb588b5ee294f57c4e86707`. |
| Many references, one responsibility each | Image-reference mode does not mean that the first listed image is a starting frame or that static pixels own dynamic events. | Give every reference an explicit preserve/complete/ignore and `doesNotControl` boundary; keep first/last frame null unless a literal boundary frame is intended. | P01A r49 compiles nine references: scene look, three identities, single/group corpse identity, and three Director blocking states; dynamics remain in the 24fps plan and detailed Prompt. | Compilation `prompt-compilation-ccc94cf3-8426-4442-9291-eac9a68b0f27`; first/last null; technical gates pass, current signoff/TeamManifest remain blocked. |
| Single-keyframe five-reference ceiling | A nine-reference video plan cannot be copied unchanged into GPT Image single-keyframe generation. | Fail before payment above five; select a shot-specific set by semantic risk and renumber it contiguously. Never silently truncate. | P01A r3 uses scene + three heroes + corpse group; Director geometry remains in the written spatial contract and pixel review. | Initial compile blocked with `single_keyframe_reference_limit_exceeded`; five-reference compile `image-prompt-compilation-cf40ca9c-9c70-42cf-943b-2f042c0b09b6` is lint clean. |
| New candidate does not inherit old verdict | Importing a new image onto a node with a prior REJECT can leave stale rejection state attached to the current pixels. | Move the old review ID to history, preserve rejected media audit, reset current verdict to candidate and bind the new exact checksum/run. | P01A keyframe node r10 displays media `media-06bc2416-76d7-4c8a-bbce-5ab35cdb106a` as Owner-pending rather than inheriting r2 REJECT. | `storyboard-image-candidate-node-policy.mjs`; storyboard provider integration regression; full verify 326/326. |

## 2026-07-22 exact semantic keyframe promotion

| Capability | Source gap / observed failure | Required industrial behavior | Real project result | Evidence |
| --- | --- | --- | --- | --- |
| Accepted still is not a literal first frame | A strong whole-shot image can stabilize people, scene and space, but treating it as t0 freezes or misdirects the intended action. | Bind exact media/checksum/review/Shot proof as `storyboard_composition`; state `doesNotControl`; keep first/last null; drive events, camera speed, performance and H1 from the temporal Prompt. | P01A r50 uses the accepted r3 still first, three hero identities, single/group corpse identity and one compiler-added Director binding. Seven references pass technical preflight without any frame input. | Review `review-8b8431e4-ca73-470c-af2d-e2ef54a7a37b`; storyboard r115; Unit r50; compilation `prompt-compilation-cbe15954-2a43-4800-af42-f9cb5cb47145`; `stale:false`, technical preflight true. |
| Still approval does not widen production authority | Owner acceptance of one image can be mistakenly read as permission to render video or clear another shot. | Scope the review note to exact media/checksum and re-run independent signoff, Manifest, continuity and paid gates. | P01A remains blocked only by current professional/TeamManifest governance; P01B still has zero references/first frame and inherits the v4 REJECT. | P01B compilation `prompt-compilation-f0c0eb60-f6ed-4f86-bb21-490d07838b26`; no Provider call; budget CNY 54 consumed / 0 reserved. |

## 2026-07-22 focus-trajectory and current visual-carrier parity update

| Capability | Source gap / observed failure | Required industrial behavior | Real project result | Evidence |
| --- | --- | --- | --- | --- |
| Focus is a temporal camera state | Prose said “拉焦到肩背/斗篷” while the structured endpoint still focused about 8.5m away; pose and FOV checks alone could not detect the optical contradiction. | Any changing focus requires an endpoint-complete numeric curve with target and interpolation. Shot start/end, Shot curve and Unit camera states must match at every shared boundary; endpoint distance must correspond to the actual endpoint subject. | P01A Shot r50 / Unit r53 use `9.92/9.33/8.56/8.56/3.00/0.25m` at `0/1/2.4/3.3/3.7/4s`; camera and actors freeze after 2.4s while focus alone transfers to shoulder and near-lens H1. | Atomic camera/temporal policies, schema/Web types, focused 60/60, full verify 331/331. |
| Storyboard composition is a version-bound state carrier | A whole-shot semantic still is not t0, but it still supplies the pixels that claim identity, topology, blocking, composition and continuity; reusing an old Shot proof after camera/focus revision would make those facts unreviewed for the current contract. | Keep it as `storyboard_composition`, not first frame, while requiring exact media/checksum, five-domain proof, latest review and current Shot revision. Any Shot revision mismatch blocks before Provider. | The same accepted image remains visible, but its proof covers Shot r46 and is correctly stale for r50. No proof was rewritten. | API integration regression emits `visual_state_carrier_shot_stale`; compilation `prompt-compilation-2d4a7170-8331-4b73-80d1-074f3cb18937`. |
| Professional PASS does not approve a TeamManifest or pixels | A specialist may find the current content technically sound while Owner governance remains incomplete. | Persist exact-revision, knowledge-grounded, no-veto contributions, but require the manifest to be independently Owner-approved and bound; keep Shot and exact-pixel reviews separate. | director-story, cinematography, continuity-qa and screenwriter all PASS Story r7/Shot r50/Unit r53. Candidate manifest remains unbound; preflight still blocks four Owner/governance codes. | Four `professional-contribution-bloodmoon-p01a-r53-*-pass-v1` records; P01A node r104; Provider 0. |
| Actual take → canon → next-unit loop | No direct Momo contract; external MIT design evidence from `Emily2040/seedance-2.0` commit `57d01…` supplies clip-state/take-review structure, but its generation benchmark is synthetic. | Separate completed/current/reserved beats; review actual start/end and deviations; only latest ACCEPT may update canon; compile the next unit from accepted carry-forward state; schedule configurable re-anchors; make retake type decision-compatible. | Added `GenerationUnit.sequenceState`, evaluation observation/canon/retake records, production Core gate, Prompt compiler 3.4.0 state-boundary section, Schema/Web types and API/CLI persistence. Fixed depth 2/3 remains LIMITED, not a universal rule. | `cinematic-sequence-state-policy.test.mjs`, `cinematic-evaluation-gate-policy.test.mjs`, `cinematic-compilation-context.test.mjs`, `cli-production-create.test.mjs`; HTTP round-trips observation/canon/retake, CLI round-trips exact sequence state; detailed source boundary in the external orchestration audit and Skill reference. |

## 2026-07-23 continuous visual workspace parity update

| Capability | Source gap / observed failure | Required industrial behavior | Real project result | Evidence |
| --- | --- | --- | --- | --- |
| Playable sequence before generation | Storyboards and isolated Shot prompts do not give the Agent or Owner one continuous time-ordered picture, so cuts, spatial conservation and adjacent motion can be locally plausible but globally wrong. | Persist a revisioned `SequencePrevisDocument`, play one timebase, expose prior/current/next phases, and require one exact `CutDecision` per adjacent boundary before formal generation. | `sequence-previs-bloodmoon-p01a-p01b-r01` plays P01A→P01B on 0–9s and records the 4s foreground-wipe boundary; P01B remains visibly blank instead of borrowing a false frame. | New contracts/schema/local adapter/Core/API/CLI/Web workspace; `cinematic-sequence-workspace.test.mjs`; full verify 341/341. |
| Positive visual context is exact and current | A storyboard image may exist without being selected or pixel-reviewed; blindly collecting all images contaminates later Prompt and generation evidence. | Include storyboard media only when selected and pixel-reviewed; include Authority media only from accepted lineage; keep rejected examples negative-only; append a replacement context and decision trace instead of rewriting history. | Correct P01B context `visual-context-bundle-121cb...` has zero positive references; contaminated `visual-context-bundle-1a730...` is superseded by trace `creative-decision-trace-a2eeb...`. | Core integration regression plus real CLI round-trip; no direct SQLite. |
| Previs acceptance cannot be faked | A UI button or generic review endpoint can otherwise mark an incomplete animatic accepted. | Only a dedicated Core review path may ACCEPT. Require real project image media, latest pixel ACCEPT, current context for every Shot, continuous timing and exact cut boundaries. Generic sequence review must fail closed. | Real Blood Moon ACCEPT attempt returns `sequence_previs_acceptance_blocked` because P01B has no frame. No favorable review was written. | API bypass regression, official CLI review command, Web button disabled for obvious missing frame/context. |
| Skill/runtime context parity | Workflow start hashes the Skill + mandatory references and persists/refreshes `UnunuCinematicAgentContextV1` with current artifact revisions and blockers. | `skillContext` and `agentContext` are part of the persisted workflow manifest; missing creative contracts remain blockers. | `tests/cinematic-workflow.test.mjs` 3/3; full verify pending. | Core workflow context snapshot and runtime refresh; no Provider call. |
| Planned picture and actual take are distinct memories | A good Previs proves intent, not what a generated video actually did. | Persist dense phase samples and planned-vs-actual evidence in `VisualTakeMemory`; only latest accepted actual exit state may drive continuation; explain each one-variable repair in `CreativeDecisionTrace`. | Resources and CRUD paths are implemented; Blood Moon has the first context-supersession trace, while take memory remains empty until a real paid video exists. | Contracts/API/CLI tests; Skill mandatory reference; Provider 0 in this integration. |

## Cinematic workflow manifest parity（2026-07-23）

| 外部能力观察 | UnunuTV 真实落点 | 验证边界 |
|---|---|---|
| 一次输入启动完整短片流程 | `workflow cinematic-start` / `POST /cinematic-workflow/start` 创建版本化 manifest 与 13 阶段 AutomationRun | 只启动编排，不触发 Provider |
| Skill 持续读取项目状态 | manifest 写入 AutomationRun configuration，`cinematic-status` 从持久化 run/task 恢复 | 不依赖聊天记忆 |
| 图、视频、剪辑连续衔接 | SequencePrevis、VisualContextBundle、VisualTakeMemory、GenerationUnit 和 timeline 合同 | 仍需真实 accepted actual take |
| 多模型适配 | manifest provider-neutral，模型由注册 adapter/capability preflight 决定 | 不保证模型质量等价 |
| 自动产出几十秒 | DAG 可编排多段 Provider segment 与剪辑 | Canonical path uses preflight-then-auto-dispatch through the Provider account; creative ACCEPT and actual-take QA remain independent. |

结论：吸收的是可执行状态机和恢复协议，不是外部 Skill/runtime 的复制；没有 Provider 调用或外部工作流接入。

| Owner-start closes only exact current gates | A one-click request can be mistaken for permission to reuse stale expert reviews or to confuse technical readiness with creative acceptance. | Re-sign the exact Story/Shot/Unit revisions, bind the Owner-approved TeamManifest through production state, compile the Prompt Draft and preflight, then dispatch through the Provider account. No separate spend approval is part of the canonical path. | P01A r58/r56/r7/r188 has current director-story/cinematography/continuity-qa PASS, production revision 6 binds `team-a61f0664e7b9e9d0685f`, and compilation `prompt-compilation-762186e8-45eb-4dd2-9e30-faf982de42e7` is `lint.ok=true / preflight.ok=true / ready=true / stale=false`; no Provider call. | Official CLI contribution/production update/unit compile+preflight; P01B remains blocked. |
| Preflight-to-canvas projection parity | A ready Core preflight can be hidden by an old blocked node snapshot, causing the director台 to disagree with the source of truth. | Project the exact preflight result back through Core policy/use-case, carrying compilation ID, source revisions, blockers and Provider state; never let Web infer readiness. | P01A node r105 shows `preflight_ready`, Unit r58 / Shot r56 / Story r7, compilation `prompt-compilation-762186e8-45eb-4dd2-9e30-faf982de42e7`, `blockers=[]`, `providerCalled=false`; Web label is “预检通过”. | `projectGenerationUnitPreflightToNode` + `syncGenerationUnitPreflightNode`; focused 12/12, full 348/348, Architecture and Next.js build pass. |

| Effective reference manifest parity | A node can show only library-resolvable cards while the compiler transiently adds storyboard/director references to the Provider request. | Compiled envelope bindings == persisted canvas effective bindings == Provider `referenceMediaIds` in the same order; derived scene/composition/camera references remain visible and locked even without an asset-library row. Prompt placeholders must resolve to the same provider index. | P01A incident fixture exposed four visible cards versus seven effective refs. Formal runs now persist a pinned PromptDocument and fail before Provider on mismatch. | `auditCompiledProviderReferenceSet`; focused manifest/prompt/run/compilation 25/25; final full verify 354/354, Architecture and production build pass. |

| Flova/Seedance Prompt Draft loop | External Skills use an ordered state loop, reference roles, prompt draft, preflight, actual-take observation and canon update; they do not rely on chat memory or one giant prompt. | Ununu now stores `CinematicPromptDraftV1` in each `CinematicPromptEnvelopeV2`, with source revisions, ordered sections, references, motion/camera inputs, negative constraints and preflight status; formal runs require exact Draft/envelope equality. | Canonical cinematic workflow is forced to `preflight_then_auto_dispatch` + `provider_account`; retry strategy is persisted separately from legacy budget grants. | Focused workflow/generation/API/automation regression 28/28; final full verify 354/354; Architecture and production build pass; no Provider call. |

## 2026-07-23 Reference-mode and no-template parity closure

| External pattern | UnunuTV executable equivalent | Hard failure prevented |
| --- | --- | --- |
| Agent passes structured creative packet, runtime owns DAG | Canonical entry persists supplied StoryPacket/VisualBible/script rows; bootstrap validates and never writes generic creative defaults | “brief → 主角/固定机位/占位对白” garbage |
| Still references stabilize identity/scene while Prompt drives motion | Selected `storyboard_composition` becomes a complete ReferenceBinding with `doesNotControl` for time/action/camera | Reference image being mistaken for t0 or freezing the clip |
| Frame input is a distinct temporal mode | `first_frame`/`first_last_frame` carry only literal boundaries and are checked against model capability/mutual exclusion | Ordinary refs silently mixed into first/last-frame payload |
| Generated takes are observed before canon update | Executor writes a video candidate only; continuity waits for a real latest evaluation | Provider success being falsely promoted to ACCEPT |

This is a source-to-runtime parity record, not a claim that an absent creative
LLM or absent media can be invented. If the structured upstream packet or real
anchor is missing, the correct output is a durable blocker.

## 2026-07-23 一次性修复最终执行闭环

`unit design` 的手动入口与 13-stage automation 共享同一个参考输入解析器：用户导入的角色/场景/道具绑定、画布选中的 storyboard composition、显式 first/last frame 和 text-only 都先归一化为完整 `ReferenceBinding` manifest；普通语义参考不会变成首帧，首尾帧不会混入普通参考。没有完整绑定或声明锚点却没有真实媒体时，Core 在 Provider 前阻断。

代码职责已原子化为 `application-foundation-use-cases.mjs` 与 `automation-stage-executor.mjs`，保持同一 Core/API mutation path。最终 `npm test` **367/367**、`npm run build`、`npm run verify:arch` 均通过。无 Provider 调用，项目状态没有因本轮源码验证被改写。
